<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_bg_rounded"
    android:padding="12dp">

    <!-- Visualizer on the right -->
    <LinearLayout
        android:id="@+id/widget_viz_container"
        android:layout_width="50dp"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="4dp">
        <View android:id="@+id/viz_bar1" android:layout_width="6dp" android:layout_height="20dp" android:layout_marginHorizontal="1dp" android:background="#BB86FC" />
        <View android:id="@+id/viz_bar2" android:layout_width="6dp" android:layout_height="35dp" android:layout_marginHorizontal="1dp" android:background="#9D4EDD" />
        <View android:id="@+id/viz_bar3" android:layout_width="6dp" android:layout_height="15dp" android:layout_marginHorizontal="1dp" android:background="#BB86FC" />
        <View android:id="@+id/viz_bar4" android:layout_width="6dp" android:layout_height="28dp" android:layout_marginHorizontal="1dp" android:background="#9D4EDD" />
        <View android:id="@+id/viz_bar5" android:layout_width="6dp" android:layout_height="40dp" android:layout_marginHorizontal="1dp" android:background="#BB86FC" />
    </LinearLayout>

    <!-- Album art on the left -->
    <ImageView
        android:id="@+id/widget_album_art"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:scaleType="centerCrop"
        android:background="@drawable/widget_art_bg"
        android:contentDescription="@string/album_art" />

    <!-- Center info + controls -->
    <LinearLayout
        android:id="@+id/widget_info_section"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/widget_album_art"
        android:layout_toStartOf="@id/widget_viz_container"
        android:layout_centerVertical="true"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="4dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/widget_song_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/no_song_playing"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/widget_artist_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/unknown_artist"
            android:textColor="#B3FFFFFF"
            android:textSize="11sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:layout_marginTop="2dp" />

        <ProgressBar
            android:id="@+id/widget_progress"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="3dp"
            android:layout_marginTop="8dp"
            android:progress="0"
            android:max="1000"
            android:progressTint="#BB86FC"
            android:progressBackgroundTint="#33FFFFFF" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:orientation="horizontal"
            android:gravity="start">

            <ImageButton
                android:id="@+id/widget_btn_prev"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:src="@drawable/ic_widget_prev"
                android:background="@android:color/transparent"
                android:tint="#FFFFFF"
                android:contentDescription="@string/previous" />

            <ImageButton
                android:id="@+id/widget_btn_play_pause"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_marginHorizontal="8dp"
                android:src="@drawable/ic_widget_play"
                android:background="@drawable/widget_play_btn_bg"
                android:tint="#FFFFFF"
                android:contentDescription="@string/play_pause" />

            <ImageButton
                android:id="@+id/widget_btn_next"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:src="@drawable/ic_widget_next"
                android:background="@android:color/transparent"
                android:tint="#FFFFFF"
                android:contentDescription="@string/next" />

            <ImageButton
                android:id="@+id/widget_btn_favorite"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_marginStart="8dp"
                android:src="@drawable/ic_widget_heart"
                android:background="@android:color/transparent"
                android:tint="#FF4081"
                android:contentDescription="@string/favorite" />

            <ImageButton
                android:id="@+id/widget_btn_queue"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_marginStart="4dp"
                android:src="@drawable/ic_widget_queue"
                android:background="@android:color/transparent"
                android:tint="#B3FFFFFF"
                android:contentDescription="@string/queue" />
        </LinearLayout>
    </LinearLayout>
</RelativeLayout>
